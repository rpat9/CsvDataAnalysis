# CsvDataAnalysis
Command line C++ application that allows a user to load a csv file into memory, inspect it contents, and run queries against the data without a database engine or external libraries
